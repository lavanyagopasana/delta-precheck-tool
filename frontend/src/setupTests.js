// Loaded automatically by react-scripts (Jest) before every test file. Adds the custom
// jest-dom matchers (toBeInTheDocument, toBeDisabled, etc.) used across the suite.
import "@testing-library/jest-dom";
